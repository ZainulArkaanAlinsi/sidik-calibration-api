# Monday, 13 July 2026

⬅️ [[Minggu 01 - Setup & Fondasi]] · 🏠 [[Dashboard]]

*(hari pertama)* | [[2026-07-14|Hari berikutnya →]]

**Minggu 1 Hari ke-1** — Setup & Fondasi

## Fokus Hari Ini
Hari pertama: infrastruktur kosong jadi ada titik mulai.

## Tugas Backend (PIC: Zainul)
- [x] Buat repo `asmo-api` (Laravel), setup `.env`, install dependency dasar.

## Tugas Mobile (PIC: *isi nama*)
- [x] Buat repo `asmo-mobile` (Flutter), setup project, putuskan state management (Riverpod/Provider).

## Target Akhir Hari (Definition of Done)
Kedua repo ada, bisa di-run lokal (`php artisan serve` & `flutter run`).

## Progress Hari Ini (isi manual sebelum pulang)
**Backend (`asmo-api`)**
- Repo Laravel 13 (PHP 8.3) jalan, sudah ada branch `main` + `develop`, remote ke GitHub.
- Dependency dasar: **Laravel Sanctum** (auth token buat mobile) — `HasApiTokens` sudah nempel di model `User`, migrasi `personal_access_tokens` ada, route `/api` aktif.
- `.env` dikonfigurasi: `APP_NAME="ASMO API"`, `APP_TIMEZONE=Asia/Jakarta`, `APP_LOCALE=id`, DB diarahkan ke **MySQL** (`asmo_db`).
- `config/app.php`: timezone dibikin env-driven (`env('APP_TIMEZONE', 'UTC')`) — sebelumnya di-hardcode `'UTC'` jadi setting di `.env` nggak kebaca.
- `README.md` diganti dari bawaan Laravel → README project: tech stack, setup lokal, konvensi API, ringkasan aturan bisnis, git workflow.
- Verifikasi: `php artisan route:list` jalan, config kebaca (`ASMO API | Asia/Jakarta | db=mysql`).

**Mobile (`asmo-mobile`)**
- Repo Flutter jalan, remote ke GitHub.
- **State management: Riverpod** (`flutter_riverpod ^3.3.2` sudah masuk `pubspec.yaml`).

**Vault**
- Vault Obsidian `Project-PT-ASMO/` di-track di repo `asmo-api` (di `asmo-mobile` di-gitignore) — biar cuma ada satu sumber kebenaran, nggak dobel & nggak diverge.

## Blocker / Catatan
- **Belum kelar: `php artisan migrate`.** Database `asmo_db` belum dibuat karena MySQL 8.0 di laptop (port 3306) root-nya berpassword dan passwordnya belum diketahui. Note: XAMPP MySQL juga keinstall tapi nggak jalan (port-nya sudah dipakai MySQL 8.0). Begitu password-nya ada → bikin DB, isi `DB_PASSWORD` di `.env`, `php artisan migrate`, baru DoD hari ini 100%.
- PIC mobile belum diisi namanya.
